{
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10"
  },
  "private": true,
  "scripts": {
    "install:client": "npm --prefix client install",
    "install:server": "npm --prefix server install",
    "build": "npm run install:client && npm run install:server && npm --prefix client run build"
  }
}
